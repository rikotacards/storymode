import { Typography } from '@mui/material';
import React from 'react';

export const Comments: React.FC = () => {
  return (
    <div>
      <Typography typography='caption'>View all 5 comments</Typography>
    </div>
  )
}